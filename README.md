# mp3_to_mp4
A simple shell script that uses ffmpeg to convert mp3 files to mp4 for easy uploading to Youtube

## Usage
`mp3_to_mp4 -i cover.jpg -f /path/to/audio/directory -o /path/to/output/directory`

## Dependencies
ffmpeg
